//Name: Khushal Dhingra
//ID: 1225420820
#ifndef _heap_h
#define _heap_h

#include "data_structures.h"
/*
// Define the ELEMENT struct
typedef VERTEX ELEMENT;
typedef ELEMENT* pELEMENT;

typedef struct TAG_ELEMENT {
    double key;
    int value;
};/ELEMENT;

// Define the HEAP struct
typedef struct TAG_HEAP {
    int capacity;
    int size;
    int numVertices;
    pELEMENT* H;
}HEAP;

typedef HEAP* pHEAP;


void MinHeapify(pHEAP heap, int i);
void BuildMinHeap(pHEAP heap);
void HeapDecreaseKey(pHEAP heap, int i, double key);
ELEMENT* HeapExtractMin(HEAP* heap);
//pVERTEX HeapExtractMin(pHEAP heap);
void MinHeapInsert(pHEAP heap, pVERTEX vertex);


int main() {
    // Example usage
    HEAP heap; // Declare a heap instance

    // Initialize the heap's fields
    heap.capacity = 100;
    heap.size = 0;
    heap.numVertices = 0;
    heap.H = new pELEMENT[heap.capacity]; // Allocate memory for the array of pELEMENTs

    // Use the heap...

    // Don't forget to free memory when done
    delete[] heap.H;

    return 0;
}

*/
#endif 
